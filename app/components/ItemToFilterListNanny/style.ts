import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    labels: {
        flexDirection: 'row',
        gap: 10,
        marginRight: 25,
        flex: 1,
        backgroundColor: 'white',
        padding: 15,
        height: 55,
        borderRadius: 20,
    },
})