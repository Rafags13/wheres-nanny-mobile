import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    labels: {
        flexDirection: 'row',
        gap: 10,
        marginRight: 25,
        backgroundColor: 'white',
        padding: 15,
        borderRadius: 20
    },
})