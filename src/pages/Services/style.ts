import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    personPhoto: {
        width: 50,
        height: 50,
        marginRight: 10
    },
    dataContainer: {
        backgroundColor: '#78BCF1',
        flexDirection: 'row',
        alignItems: 'center',
        gap: 10,
        padding: 10,
        borderRadius: 15
    },
})