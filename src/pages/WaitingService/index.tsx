import { View } from "react-native";

export default function WaitingService() {
    return (
        <View>
            {/* Create a page to make user waiting for response that service */}
        </View>
    )
}